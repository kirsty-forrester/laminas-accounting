<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                          https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="Accounting\Model\JournalEntry" table="journal_entry">
        <id name="journalEntryId" type="integer" column="journal_entry_id">
            <generator strategy="IDENTITY"/>
        </id>

        <field name="date" type="date_immutable" column="date"/>
        <field name="description" type="string" column="description"/>
        <field name="status" type="string" column="status"
               enum-type="Accounting\ValueObject\JournalEntryStatus"/>

        <!-- Inverse side of the aggregate. Cascade persist/remove + orphan removal
             mean the lines live and die with the entry -->
        <one-to-many field="lines"
                     target-entity="Accounting\Model\JournalEntryLine"
                     mapped-by="journalEntry"
                     orphan-removal="true">
            <cascade>
                <cascade-persist/>
                <cascade-remove/>
            </cascade>
        </one-to-many>
    </entity>
</doctrine-mapping>
