<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                          https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="Accounting\Model\Account" table="account">
        <id name="accountId" type="integer" column="account_id">
            <generator strategy="IDENTITY"/>
        </id>

        <field name="name" type="string" column="name"/>

        <!-- enum-type tells Doctrine to convert the stored string
             ('asset', 'liability', …) to/from your PHP enum automatically. -->
        <field name="type" type="string" column="type"
               enum-type="Accounting\ValueObject\AccountType"/>
    </entity>
</doctrine-mapping>
